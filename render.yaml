services:
  - type: web
    name: smart-trolley
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server_app:app --bind 0.0.0.0:10000 --workers 1 --timeout 120
    envVars:
      - key: PORT
        value: 10000