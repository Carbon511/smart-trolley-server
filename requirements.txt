# SmartTrolley — Render Server
# All inference runs here. Pi only sends frames + weight.

flask==3.0.0
gunicorn==21.2.0

# Vision + ONNX inference (runs on Render)
opencv-python-headless==4.8.1.78
numpy==1.24.3
onnxruntime==1.17.1
onnx==1.15.0

# Payment
razorpay==1.4.1

# WhatsApp — WATI uses plain requests (already below)
# WhatsApp — Twilio fallback
twilio==8.10.0

# HTTP client (used by WATI calls)
requests==2.31.0

# Bill generation
reportlab==4.0.7

# Utilities
python-dotenv==1.0.0
Pillow==10.1.0